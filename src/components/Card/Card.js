import Carousel from '../Carousel/Carousel';
import './Card.scss';

function Card() {
    return (
        <div className='mock-frame'>
            <Carousel />
        </div>
    );
}

export default Card;
